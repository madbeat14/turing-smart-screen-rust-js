// GPU sensor data is provided by LhmService via the shared LhmSensorData state.
// All GPU readings (temp, usage, freq, VRAM) come from lhm.rs -> sensors/mod.rs.
// This module is intentionally empty — kept as a placeholder for future direct GPU sensor access.
