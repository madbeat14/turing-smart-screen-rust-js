// GPU sensor data is now provided by LhmService via the shared LhmSensorData state.
// This module is kept as a placeholder for the GpuData type if needed in the future.
// All GPU readings (temp, usage, freq, VRAM) come from lhm.rs → sensors/mod.rs.
